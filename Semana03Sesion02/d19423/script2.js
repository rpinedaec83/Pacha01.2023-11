let arrEntradasGenerales = [];

funtion generarUUID(){
    return "100000000-1000-4000-8000-10000000",replace(/)
}
let nombre = promp("dime tu nombre"); // el promp es la ventanita con el mensaje
let sector = promp("escribe el sector [general  o vip]"); // mi ventanita co el mensaje y mis dos opciones
let codigo = generarUUID()
let objEntrada = { // las llavesitas para validar objetos
    nombre,
    sector,
    codigo
};

arrEntradasGeneradas.push(objEntrada)
let respuesta = prompt ["quiere generar otra[si - no]").toUppercase();
if (respuesta !=="SI"){ 
    bandera = false;
}

funtion eliminarEntradasGeneradas(codigo){
for(var i=0;i <arrEntradasGeneradas.length)


}
 generarEntrada()

 function validarEntrada {
    let codigo = prompt ("ingresa tu codigo de entrada");
    let obj=arrEntradasGeneradas.find(0=>0.codigo===codigo);
    console.log(obj)
    if(obj !== undefined){
        aaEntradasValidadas.push(obj);
        let divMensaje = document.getElementById("Mensaje");
        divMensaje.style.display="block";
        let datosEntrada = document.getElementById
    }
    console.log(codig0)
 }