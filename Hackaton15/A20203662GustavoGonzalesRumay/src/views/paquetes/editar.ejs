<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Editar Paquete</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h2>Editar Paquete</h2>
        <form action="/paquetes/actualizar/<%= paquete._id %>" method="POST">
            <div class="form-group">
                <label for="descripcion">Descripción:</label>
                <input type="text" class="form-control" id="descripcion" name="descripcion" value="<%= paquete.descripcion %>" required>
            </div>
            <div class="form-group">
                <label for="estadoTransito">Estado:</label>
                <select class="form-control" id="estadoTransito" name="estadoTransito">
                    <option value="En tránsito" <%= paquete.estadoTransito === 'En tránsito' ? 'selected' : '' %>>En tránsito</option>
                    <option value="Entregado" <%= paquete.estadoTransito === 'Entregado' ? 'selected' : '' %>>Entregado</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Actualizar</button>
        </form>
    </div>
</body>
</html>
